% ==================================================
% Anexos / Apéndices
% ==================================================

% Convertimos los capítulos siguientes a anexos con letras (A, B, C)
% y forzamos el rótulo "Anexo" en lugar de "Capítulo".
\appendix
% En clases book/report, \appendix cambia el rótulo a \appendixname.
% Con babel (español) suele ser "Apéndice"; lo forzamos a "Anexo".
\renewcommand{\appendixname}{Anexo}
\renewcommand{\chaptername}{Anexo}

% --------------------------------------------------
% ANEXO A: Imágenes del sistema
% --------------------------------------------------
\newpage
\chapter{Imágenes del sistema (SerenityOS)}\label{anexo:imagenes}

Este anexo presenta capturas del entorno gráfico y de aplicaciones base de SerenityOS, con el fin de evidenciar la ejecución del sistema y su interfaz de usuario.

\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/serenity-arranque.png}
\caption{Arranque de SerenityOS en ejecución (evidencia de inicio del sistema).}\label{fig:anexo-a-arranque}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/serenity-escritorio.png}
\caption{Escritorio y entorno gráfico principal de SerenityOS.}\label{fig:anexo-a-escritorio}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/serenitiy-CLI.png}
\caption{Terminal de SerenityOS (línea de comandos) para interacción técnica.}\label{fig:anexo-a-terminal}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/serenitiy-filemanager.png}
\caption{Administrador de archivos de SerenityOS (navegación del sistema de archivos).}\label{fig:anexo-a-filemanager}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/serenity-settings.png}
\caption{Panel de configuración (Settings) de SerenityOS.}\label{fig:anexo-a-settings}
\end{figure}

\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/serenity-browser.png}
\caption{Navegador web en SerenityOS como ejemplo de aplicación de usuario.}\label{fig:anexo-a-browser}
\end{figure}

% --------------------------------------------------
% ANEXO B: Código importante
% --------------------------------------------------
\newpage
\chapter{Códigos importantes}\label{anexo:codigo}

\section{Código de procesos (Process/Thread)}
{\small
\begin{verbatim}
struct LoadResult;

class Process final
	: public ListedRefCounted<Process, LockType::Spinlock>
	, public LockWeakable<Process> {

	class ProtectedValues {
	public:
		ProcessID pid { 0 };
		ProcessID ppid { 0 };
		// FIXME: This should be a NonnullRefPtr
		RefPtr<Credentials> credentials;
		RefPtr<ProcessGroup> process_group;
		RefPtr<TTY> tty;
		bool dumpable { false };
		bool executable_is_setid { false };
		bool has_promises { false };
		u32 promises { 0 };
		bool has_execpromises { false };
		u32 execpromises { 0 };
		mode_t umask { 022 };
		VirtualAddress signal_trampoline;
		Atomic<u32> thread_count { 0 };
		u8 termination_status { 0 };
		u8 termination_signal { 0 };
		SetOnce reject_transition_to_executable_from_writable_prot;
		SetOnce jailed_until_exit;
		bool jailed_until_exec { false };
	};

public:
	AK_MAKE_NONCOPYABLE(Process);
	AK_MAKE_NONMOVABLE(Process);

	MAKE_ALIGNED_ALLOCATED(Process, PAGE_SIZE);

	friend class Thread;

	auto with_protected_data(auto&& callback) const
	{
		SpinlockLocker locker(m_protected_data_lock);
		return callback(m_protected_values_do_not_access_directly);
	}

	auto with_mutable_protected_data(auto&& callback)
	{
		SpinlockLocker locker(m_protected_data_lock);
		unprotect_data();
		auto guard = ScopeGuard([&] { protect_data(); });
		return callback(m_protected_values_do_not_access_directly);
	}
\end{verbatim}
}

\section{Código del scheduler (planificación)}
{\small
\begin{verbatim}
enum class ScheduleResult {
	Success,
	NoRunnableThreadFound,
	Delayed,
};

class Scheduler {
public:
	static void initialize();
	static Thread* create_ap_idle_thread(u32 cpu);
	static void set_idle_thread(Thread* idle_thread);
	static void timer_tick();
	[[noreturn]] static void start();
	static ScheduleResult pick_next();
	static ScheduleResult yield();
	static void context_switch(Thread*);
	static void enter_current(Thread& prev_thread);
	static void leave_on_first_switch(InterruptsState);
	static void prepare_after_exec();
	static void prepare_for_idle_loop();
	static Process* colonel();
	static void idle_loop(void*);
	static void invoke_async();
	static void notify_finalizer();
	static Thread& pull_next_runnable_thread();
	static Thread* peek_next_runnable_thread();
	static bool dequeue_runnable_thread(Thread&, bool = false);
	static void enqueue_runnable_thread(Thread&);
	static void dump_scheduler_state(bool = false);
	static bool is_initialized();
};
\end{verbatim}
}

\section{Código de gestión de memoria (MemoryManager / VM)}
{\small
\begin{verbatim}
// This class represents a set of committed physical pages.
// When you ask MemoryManager to commit pages for you, you get one of these in return.
// You can allocate pages from it via `take_one()`
// It will uncommit any (unallocated) remaining pages when destroyed.
class CommittedPhysicalPageSet {
	AK_MAKE_NONCOPYABLE(CommittedPhysicalPageSet);

public:
	CommittedPhysicalPageSet(Badge<MemoryManager>, size_t page_count)
		: m_page_count(page_count)
	{
	}

	CommittedPhysicalPageSet(CommittedPhysicalPageSet&& other)
		: m_page_count(exchange(other.m_page_count, 0))
	{
	}

	~CommittedPhysicalPageSet();

	bool is_empty() const { return m_page_count == 0; }
	size_t page_count() const { return m_page_count; }

	[[nodiscard]] NonnullRefPtr<PhysicalRAMPage> take_one();
	void uncommit_one();

	void operator=(CommittedPhysicalPageSet&&) = delete;

private:
	size_t m_page_count { 0 };
};

class MemoryManager {
public:
	PageFaultResponse handle_page_fault(PageFault const&);
	enum class ShouldZeroFill {
		No,
		Yes
	};
	ErrorOr<CommittedPhysicalPageSet> commit_physical_pages(size_t page_count);
\end{verbatim}
}

\section{Código relacionado a syscalls de memoria (mmap/munmap/mprotect)}
{\small
\begin{verbatim}
ErrorOr<FlatPtr> sys$mmap(Userspace<Syscall::SC_mmap_params const*>);
ErrorOr<FlatPtr> sys$mremap(Userspace<Syscall::SC_mremap_params const*>);
ErrorOr<FlatPtr> sys$munmap(Userspace<void*>, size_t);
ErrorOr<FlatPtr> sys$set_mmap_name(Userspace<Syscall::SC_set_mmap_name_params const*>);
ErrorOr<FlatPtr> sys$mprotect(Userspace<void*>, size_t, int prot);
ErrorOr<FlatPtr> sys$madvise(Userspace<void*>, size_t, int advice);
ErrorOr<FlatPtr> sys$msync(Userspace<void*>, size_t, int flags);
\end{verbatim}
}

% --------------------------------------------------
% ANEXO C: Manual de usuario
% --------------------------------------------------
\newpage
\chapter{Manual de usuario y ejecución (SerenityOS)}\label{anexo:manual}

Este anexo se divide en: (i) comandos mínimos de ejecución en el host para compilar/ejecutar SerenityOS, y (ii) un resumen de guía de usuario basado en el \emph{User guide} oficial del wiki.

\section{Comandos de ejecución (host Linux)}
\subsection{Dependencias (ejemplo en Arch Linux)}
{\small
\begin{verbatim}
sudo pacman -S --needed \
	base-devel cmake curl mpfr libmpc gmp e2fsprogs ninja qemu-desktop \
	qemu-system-aarch64 ccache rsync unzip
\end{verbatim}
}

\subsection{Clonado, compilación y ejecución}
{\small
\begin{verbatim}
git clone https://github.com/SerenityOS/serenity.git
cd serenity

# Compila y ejecuta SerenityOS en QEMU.
Meta/serenity.sh run

# Compila sin arrancar la VM.
Meta/serenity.sh build
\end{verbatim}
}

\section{Manual de usuario (resumen del SerenityOS Wiki)}

Contenido basado en la sección \emph{User guide} del wiki de SerenityOS.

\subsection{Atajos de teclado}
\begin{center}
\begin{tabular}{ll}
	oprule
	extbf{Atajo} & \textbf{Acción} \\
\midrule
{\ttfamily Ctrl + Alt + Space} & Abrir selector de emojis \\
{\ttfamily Super + Space} & Abrir asistente \\
{\ttfamily F2} & Renombrar archivo \\
{\ttfamily Super + Down} & Minimizar ventana \\
{\ttfamily Super + Up} & Maximizar ventana \\
{\ttfamily Super + Left} & Acoplar ventana a la izquierda \\
{\ttfamily Super + Right} & Acoplar ventana a la derecha \\
{\ttfamily Super + Tab} & Cambiar entre ventanas \\
{\ttfamily Shift + Alt (mantener)} & Alternar keymaps (mantener Shift y presionar Alt) \\
{\ttfamily Ctrl + Shift + A} & Paleta de comandos (dentro de aplicaciones) \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Configuración (Settings)}
La ventana \emph{Settings} contiene varias subcategorías, entre ellas:
\begin{itemize}
\item \textbf{Browser:} personalizar opciones del navegador.
\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/browser.png}
\caption{Settings: Browser.}\label{fig:anexo-c-settings-browser}
\end{figure}
\item \textbf{Clock:} elegir formato de hora y zona horaria.
\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/clock.png}
\caption{Settings: Clock.}\label{fig:anexo-c-settings-clock}
\end{figure}
\item \textbf{Display:} cambiar fondo, temas, fuentes, tamaño del monitor y número de espacios de trabajo virtuales.
\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/display.png}
\caption{Settings: Display.}\label{fig:anexo-c-settings-display}
\end{figure}
\item \textbf{Keyboard:} actualizar propiedades del teclado y el \emph{keymap}.
\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/keyboard.png}
\caption{Settings: Keyboard.}\label{fig:anexo-c-settings-keyboard}
\end{figure}
\item \textbf{Mail:} definir valores necesarios para que funcione el cliente de correo.
\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/mail.png}
\caption{Settings: Mail.}\label{fig:anexo-c-settings-mail}
\end{figure}
\item \textbf{Mouse:} modificar propiedades del mouse y personalizar el tema del cursor.
\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/mouse.png}
\caption{Settings: Mouse.}\label{fig:anexo-c-settings-mouse}
\end{figure}
\item \textbf{Terminal:} ajustar comportamiento, disposición (layout) y estilo de fuente.
\begin{figure}[H]
\centering
\includegraphics[width=0.5\textwidth]{figures/terminal.png}
\caption{Settings: Terminal.}\label{fig:anexo-c-settings-terminal}
\end{figure}
\end{itemize}
